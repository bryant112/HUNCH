# HUNCH

Standalone single-player perception addon. Never modify map repositories, installed Workshop mods, existing launcher presets, or running Arma processes.
All project writes stay under this checkout outside OneDrive.
Use `python tools/check.py`, `python tools/build.py`, and the documented SQF-VM command when available.
Keep HUD records free of live source-object references. Never alter damage, faction relationships, AI knowledge, or sound playback.
Do not claim rendered-client acceptance or performance gates without current measured evidence.
